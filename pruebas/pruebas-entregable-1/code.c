#include <stdlib.h>
#include <float.h>
#include <stdio.h>

#define N 4

int main()
{
	double minimo_test = -500;
	double max_a = -DBL_MAX, max_b = -DBL_MAX;
	double min_a = DBL_MAX, min_b = DBL_MAX;
	double promedio_a = 0, promedio_b = 0;

	matriz_a = malloc(N * N * sizeof(double));
	matriz_b = malloc(N * N * sizeof(double));
	matriz_c = malloc(N * N * sizeof(double));
	matriz_r = malloc(N * N * sizeof(double));
	matriz_b_t = malloc(N * N * sizeof(double));
	return 0;
}
